#!/usr/bin/env sh
set -e

branch="$(git rev-parse --abbrev-ref HEAD)"
if [ "$branch" = "main" ]; then
  echo "Direct commits to 'main' are blocked. Create a feature branch."
  exit 1
fi

pnpm run lint
