{
  "name": "echora",
  "displayName": "Echora",
  "description": "Echora is a VS Code extension that empowers your CLI agent with STT/TTS and makes it your best companion — chatting, learning, and working, always echoing to you.",
  "version": "0.0.2",
  "engines": {
    "vscode": "^1.85.0"
  },
  "publisher": "Besthope",
  "repository": {
    "type": "git",
    "url": "https://github.com/Besthope-Official/echora.git"
  },
  "categories": [
    "AI",
    "Chat",
    "Education",
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "extensionKind": [
    "ui"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "echora",
          "title": "Echora",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "echora": [
        {
          "type": "webview",
          "id": "echora.chatPanel",
          "name": "echora",
          "icon": "media/icon.svg"
        }
      ]
    },
    "icons": {
      "echora-status": {
        "description": "Echora status icon",
        "default": {
          "fontPath": "./media/icon-font/echora.woff",
          "fontCharacter": "\\f101"
        }
      }
    },
    "commands": [
      {
        "command": "echora.startDictation",
        "title": "Start Dictation (Manual)",
        "category": "Echora",
        "icon": "$(mic)"
      },
      {
        "command": "echora.stopDictation",
        "title": "Stop Dictation (Manual)",
        "category": "Echora",
        "icon": "$(debug-stop)"
      },
      {
        "command": "echora.startVoicePipeline",
        "title": "Start Voice Pipeline",
        "category": "Echora",
        "icon": "$(play)"
      },
      {
        "command": "echora.stopVoicePipeline",
        "title": "Stop Voice Pipeline",
        "category": "Echora",
        "icon": "$(stop)"
      },
      {
        "command": "echora.toggleVoicePipeline",
        "title": "Toggle Voice Pipeline",
        "category": "Echora",
        "icon": "$(mic)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "echora.toggleVoicePipeline",
          "when": "view == echora.chatPanel",
          "group": "navigation@0"
        }
      ]
    },
    "configuration": {
      "title": "Echora",
      "properties": {
        "echora.nodeSpeech.locale": {
          "type": "string",
          "default": "auto",
          "scope": "application",
          "description": "Preferred locale for voice input recognition. Use 'auto' to follow VS Code UI language, or set e.g. 'zh-CN', 'en-US'."
        },
        "echora.stt.sessionDurationMs": {
          "type": "number",
          "default": 20000,
          "minimum": 0,
          "scope": "application",
          "description": "Maximum duration (ms) for a voice input session. Set to 0 to disable auto-stop (future VAD mode). Default: 20000."
        },
        "echora.pipeline.textConsumer": {
          "type": "string",
          "enum": [
            "agent-sdk",
            "mock"
          ],
          "enumDescriptions": [
            "Use Claude Agent SDK for real AI responses.",
            "Mock consumer for local smoke testing."
          ],
          "default": "agent-sdk",
          "scope": "window",
          "description": "Text consumer backend for the voice pipeline. Use 'agent-sdk' for Claude Agent SDK integration, or 'mock' for local smoke tests."
        },
        "echora.agentSdk.tools": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "scope": "window",
          "markdownDescription": "Optional Claude Agent SDK `tools` whitelist. Leave empty to inherit default/project tool availability. Example: `[\"WebSearch\", \"WebFetch\", \"Read\", \"Write\"]`."
        },
        "echora.agentSdk.allowedTools": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "scope": "window",
          "markdownDescription": "Tool names to auto-approve via Claude Agent SDK `allowedTools`. Example: `[\"WebSearch\", \"WebFetch\"]`."
        },
        "echora.agentSdk.disallowedTools": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "scope": "window",
          "markdownDescription": "Tool names to always block via Claude Agent SDK `disallowedTools`."
        },
        "echora.agentSdk.permissionMode": {
          "type": "string",
          "enum": [
            "inherit",
            "default",
            "acceptEdits",
            "dontAsk",
            "plan",
            "bypassPermissions"
          ],
          "enumDescriptions": [
            "Use SDK/project defaults from loaded Claude settings.",
            "Prompt for dangerous operations.",
            "Auto-accept file edit operations.",
            "Never prompt; deny requests not pre-approved.",
            "Planning mode only, no tool execution.",
            "Bypass permission checks (requires dangerous skip flag)."
          ],
          "default": "inherit",
          "scope": "window",
          "markdownDescription": "Claude Agent SDK `permissionMode`. Keep `inherit` to rely on project/user `.claude` permissions."
        },
        "echora.agentSdk.allowDangerouslySkipPermissions": {
          "type": "boolean",
          "default": false,
          "scope": "window",
          "markdownDescription": "When true, allows `echora.agentSdk.permissionMode = \"bypassPermissions\"`. Use only in trusted environments."
        },
        "echora.pipeline.remote": {
          "type": "string",
          "default": "",
          "scope": "window",
          "markdownDescription": "**[Experimental]** Enable remote bridge for the voice pipeline. This feature is under development. Format: `wsl:<distro>` (e.g. `wsl:Ubuntu`) or `ssh:<host>`. Leave empty to run locally."
        },
        "echora.pipeline.enableTextEditingBeforeSend": {
          "type": "boolean",
          "default": false,
          "scope": "window",
          "description": "When enabled, final voice transcription enters a review state in Echora sidebar and only sends after you click Send. When disabled (default), final transcription is sent immediately."
        },
        "echora.tts.enabled": {
          "type": "boolean",
          "default": true,
          "scope": "window",
          "description": "Enable text-to-speech playback for assistant final responses in the voice pipeline."
        },
        "echora.tts.backend": {
          "type": "string",
          "enum": [
            "local"
          ],
          "enumDescriptions": [
            "Use local @vscode/node-speech models provided by VS Code Speech extension."
          ],
          "default": "local",
          "scope": "window",
          "description": "Text-to-speech backend for Echora. Additional backends may be added in future versions."
        },
        "echora.pipeline.systemPrompt": {
          "type": "string",
          "default": "",
          "scope": "window",
          "editPresentation": "multilineText",
          "markdownDescription": "Custom system prompt prepended to every voice message. Leave empty to use the built-in default (concise, voice-optimized style). Overridden by `#echora.pipeline.systemPromptFile#` or a `.echora/prompt.md` file in the workspace root."
        },
        "echora.pipeline.systemPromptFile": {
          "type": "string",
          "default": "",
          "scope": "window",
          "markdownDescription": "Path to a `.md` file containing the system prompt. Relative paths resolve from workspace root. Takes priority over `#echora.pipeline.systemPrompt#` and `.echora/prompt.md`."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run package",
    "copy-webview": "node ./scripts/copy-webview-assets.mjs",
    "compile": "webpack && node ./scripts/copy-webview-assets.mjs",
    "watch": "node ./scripts/copy-webview-assets.mjs && webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map && node ./scripts/copy-webview-assets.mjs",
    "clean-tests-out": "node -e \"require('fs').rmSync('out/test', { recursive: true, force: true })\"",
    "compile-tests": "pnpm run clean-tests-out && tsc -p tsconfig.test.json",
    "watch-tests": "tsc -p tsconfig.test.json -w",
    "pretest": "pnpm run compile-tests && pnpm run compile && pnpm run lint",
    "lint": "eslint src",
    "test": "vscode-test",
    "prepare": "husky"
  },
  "vsce": {
    "dependencies": true,
    "followSymlinks": true
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.85.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.7.1",
    "eslint": "^9.39.2",
    "husky": "^9.1.7",
    "ts-loader": "^9.5.4",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.54.0",
    "webpack": "^5.104.1",
    "webpack-cli": "^6.0.1"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.59"
  }
}
